plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

//ext.GROUP = "com.lute.ble"
//ext.POM_ARTIFACT_ID = "BleCore"
//ext.VERSION_NAME = "1.4.2"
////引用gradle_upload.gradle
//apply from: "${project.rootDir}/maven_upload.gradle"

ext.GROUP = "com.github.buhuiming"
ext.POM_ARTIFACT_ID = "BleCore"
ext.VERSION_NAME = "1.4.2"
//引用gradle_upload.gradle
apply from: "${project.rootDir}/jitpack_maven_upload.gradle"

android {
    namespace 'com.bhm.ble'
    compileSdk 33

    defaultConfig {
        minSdk 24
        targetSdk 33

    }
    publishing {
        singleVariant("release")
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

dependencies {
    implementation 'androidx.lifecycle:lifecycle-common:2.6.2'
}